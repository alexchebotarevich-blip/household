// Copy this file to `Secrets.xcconfig` (excluded from source control) and provide
the environment-specific secret values. All keys defined here are optional but
recommended for local development.

FIREBASE_GOOGLE_SERVICE_PLIST = GoogleService-Info-Debug
FIREBASE_WEB_API_KEY = your-firebase-web-api-key
AMPLITUDE_API_KEY = your-analytics-api-key

// Example of referencing custom configuration values
// CUSTOM_HEADER_VALUE = dev-header-token
